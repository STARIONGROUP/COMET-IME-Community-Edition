<dx:DXWindow x:Class="CDP4VandV.Views.StageGateDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:navigation="clr-namespace:CDP4Composition.Navigation;assembly=CDP4Composition"
             Width="440"
             Height="460"
             dx:ThemeManager.ThemeName="Seven"
             navigation:ExtendedDialogResultCloser.DialogResult="{Binding DialogResult}"
             mc:Ignorable="d"
             Title="Define the stage gates">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"
                   Text="These become the stage gates of this model: the review milestones every V&amp;V activity is planned against, the columns of the coverage matrix, and the entries of the stage filter. They differ per project, so define yours now, in project order. The list below is only a common starting point; change it freely. Gates can be renamed or extended later by editing the 'V&amp;V Stage Gate' parameter type in the reference data." />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <dxg:GridControl AutoGenerateColumns="None"
                             ItemsSource="{Binding StageGates}"
                             SelectedItem="{Binding SelectedGate, Mode=TwoWay}">
                <dxg:GridControl.Columns>
                    <dxg:GridColumn FieldName="Name" Header="Stage Gate" />
                </dxg:GridControl.Columns>
                <dxg:GridControl.View>
                    <dxg:TableView AutoWidth="True"
                                   NavigationStyle="Cell"
                                   ShowGroupPanel="False"
                                   ShowIndicator="False"
                                   UseLightweightTemplates="None" />
                </dxg:GridControl.View>
            </dxg:GridControl>

            <StackPanel Grid.Column="1" Margin="6,0,0,0">
                <Button Margin="0,0,0,4" MinWidth="66" Padding="6,3" Command="{Binding AddGateCommand}" Content="Add" />
                <Button Margin="0,0,0,4" MinWidth="66" Padding="6,3" Command="{Binding RemoveGateCommand}" Content="Remove" />
                <Button Margin="0,0,0,4" MinWidth="66" Padding="6,3" Command="{Binding MoveGateUpCommand}" Content="Up" />
                <Button MinWidth="66" Padding="6,3" Command="{Binding MoveGateDownCommand}" Content="Down" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2"
                    Margin="0,10,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button MinWidth="85"
                    Margin="0,0,8,0"
                    Command="{Binding OkCommand}"
                    Content="OK"
                    IsDefault="True" />
            <Button MinWidth="85"
                    Command="{Binding CancelCommand}"
                    Content="Cancel"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</dx:DXWindow>
