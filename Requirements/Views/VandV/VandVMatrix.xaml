<UserControl x:Class="CDP4Requirements.Views.VandVMatrix"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="400"
             d:DesignWidth="800"
             dx:ThemeManager.ThemeName="{Binding Path=ThemeName}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/CDP4Composition;component/CommonView/Resources/CDP4Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <dxb:ToolBarControl Grid.Row="0" Height="30">
            <dxb:BarButtonItem Command="{Binding RefreshCommand}"
                               Content="Refresh"
                               Glyph="{dx:DXImage Image=Refresh_16x16.png}"
                               Hint="Rebuild the coverage matrix" />
        </dxb:ToolBarControl>

        <TextBlock Grid.Row="1"
                   Margin="8,6"
                   FontWeight="Bold"
                   Text="{Binding Summary}" />

        <dxg:GridControl Grid.Row="2"
                         AutoGenerateColumns="AddNew"
                         ItemsSource="{Binding MatrixTable}">
            <dxg:GridControl.View>
                <dxg:TableView Name="MatrixView"
                               AllowEditing="False"
                               AutoWidth="False"
                               ShowGroupPanel="False"
                               ShowIndicator="False" />
            </dxg:GridControl.View>
        </dxg:GridControl>
    </Grid>
</UserControl>
