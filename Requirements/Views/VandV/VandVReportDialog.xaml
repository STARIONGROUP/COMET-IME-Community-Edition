<dx:DXWindow x:Class="CDP4Requirements.Views.VandVReportDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:lc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:navigation="clr-namespace:CDP4Composition.Navigation;assembly=CDP4Composition"
             Width="560"
             Height="300"
             dx:ThemeManager.ThemeName="Seven"
             navigation:ExtendedDialogResultCloser.DialogResult="{Binding DialogResult}"
             mc:Ignorable="d"
             Title="{Binding Title}">

    <dx:DXWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/CDP4Composition;component/CommonView/Resources/CDP4Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </dx:DXWindow.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,8"
                   FontStyle="Italic"
                   Foreground="#FF5A6B7D"
                   TextWrapping="Wrap"
                   Text="A report is the document a set of V&amp;V activities is recorded in, such as a FAT report or a mass budget. It is a specification of its own, so the activities it records live inside it and it can be reviewed and exported as one document." />

        <lc:LayoutControl Grid.Row="1" Orientation="Vertical" ScrollBars="None" VerticalAlignment="Top">
            <lc:LayoutItem Label="Document Ref.: " ToolTip="The report's reference, used as its short name, e.g. FAT_REPORT_1.">
                <dxe:TextEdit EditValue="{Binding ShortName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
            </lc:LayoutItem>
            <lc:LayoutItem Label="Title: ">
                <dxe:TextEdit EditValue="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
            </lc:LayoutItem>
            <lc:LayoutItem Label="Owner: ">
                <dxe:ComboBoxEdit DisplayMember="Name"
                                  IsTextEditable="False"
                                  ItemsSource="{Binding PossibleOwners}"
                                  EditValue="{Binding Owner, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />
            </lc:LayoutItem>
        </lc:LayoutControl>

        <StackPanel Grid.Row="2"
                    Margin="0,8,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Width="90"
                    Height="26"
                    Margin="4,0"
                    Command="{Binding OkCommand}"
                    Content="Create"
                    IsDefault="True" />
            <Button Width="90"
                    Height="26"
                    Margin="4,0"
                    Command="{Binding CancelCommand}"
                    Content="Cancel"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</dx:DXWindow>
